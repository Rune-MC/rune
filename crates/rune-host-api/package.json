{
  "name": "@rune/host-api",
  "version": "0.2.0",
  "private": true,
  "description": "Trait/types crate shared by every Rune language backend.",
  "scripts": {
    "build": "cargo build --release -p rune-host-api",
    "clean": "cargo clean -p rune-host-api"
  }
}
