{
  "name": "@rune/loader",
  "version": "0.2.0",
  "private": true,
  "description": "cdylib that the Paper plugin loads via Panama FFM.",
  "scripts": {
    "build": "cargo build --release -p rune-loader",
    "clean": "cargo clean -p rune-loader"
  },
  "dependencies": {
    "@rune/host-api":     "workspace:*",
    "@rune/runtime-node": "workspace:*"
  }
}
