{
  "name": "@rune/plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Paper plugin (Kotlin + Panama FFM) -- the JVM-side of Rune.",
  "scripts": {
    "build": "node ../tools/run-gradle.mjs shadowJar --no-daemon",
    "clean": "node ../tools/run-gradle.mjs clean      --no-daemon",
    "test":  "node ../tools/run-gradle.mjs test       --no-daemon"
  },
  "dependencies": {
    "@rune/loader":        "workspace:*",
    "@rune/runtime-node":  "workspace:*"
  }
}
